<!DOCTYPE html>
<% query_hash = params.env['rack.request.query_hash'] %>

<html lang="en">
  <head>
    <title>ETT DE Leaderboard</title>
    <%= favicon %>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <% unless query_hash['auto_refresh'].nil? %>
      <meta http-equiv="refresh" content="10" />
    <% end %>

    <%= stylesheet 'bootstrap.min' unless ENV['HANAMI_ENV'] == 'production' %>
    <link href="https://vlt81.de/assets/bootstrap.min.css" rel="stylesheet">
  </head>

  <body class="bg-light">
    <%= javascript 'bootstrap.bundle.min' unless ENV['HANAMI_ENV'] == 'production' %>
    <script src="https://vlt81.de/assets/bootstrap.min.js"></script>

    <div class="container-fluid bg-light">
      <%= yield %>
    </div>
  </body>
</html>
