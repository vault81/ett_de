#!/bin/env ruby
require './config/boot'

$LOAD_PATH << './lib'

require 'refresh_players'

puts 'Starting worker'
if ENV['REFRESH_STATUS']=='1234'
  RefreshPlayers.run_status
else
  RefreshPlayers.run
end
